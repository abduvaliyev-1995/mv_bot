# from aiogram.dispatcher.middlewares import BaseMiddleware
# import logging
# from aiogram import types
# from aiogram.dispatcher.handler import CancelHandler
#
# class BigBrother(BaseMiddleware):
#     async def on_pre_process_update(self, update: types.Update, dict: dict):
        # if update.message:
        #     user = update.message.from_user.id
        #     await update.message.answer("xato Bu handler")
        # elif update.callback_query:
        #     await update.callback_query.reply_text("Update")
        # else:
        #     return

        # if True:
        #     # await update.message.answer("Qayta urinib ko'r", disable_web_page_preview=True)
        #     raise CancelHandler()





